/** 포인터를 이용하여 변수 값 바꾸기 함수 */
// *a, *b : 값
// a, b : 주소
// 변수 값이 아닌 주소의 값으로 이해. 
//a와 b를 바꾸는 것이 아닌 주소 a의 값과 주소 b의 값을 바꾸는것.

//----------------------------
void right_swap( int * a, int * b) {
	int temp;

	temp = *a;
	*a = *b;
	*b = temp;
}
